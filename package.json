{
  "name": "d2l-typography",
  "description": "Polymer-based web components and shared styles for applying D2L typography",
  "private": true,
  "scripts": {
    "galen": "npm run test:galen:sauce",
    "postinstall": "bower install",
    "test": "polylint -i d2l-typography-shared-styles.html && npm run galen",
    "galen:local:run": "d2l-galen test typography.test.js -g factory:local",
    "galen:sauce:run": "sc-run -- d2l-galen test typography.test.js -g factory:sauce -i SAUCE_USERNAME SAUCE_ACCESS_KEY TRAVIS_REPO_SLUG TRAVIS_BUILD_NUMBER -- -p4",
    "test:galen:local": "concurrently -p name -n serve,galen -s first -k \"polymer serve\" \"npm run galen:local:run\"",
    "test:galen:sauce": "concurrently -p name -n serve,galen -s first -k \"polymer serve\" \"npm run galen:sauce:run\""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/BrightspaceUI/typography.git"
  },
  "author": "D2L Corporation",
  "license": "Apache-2.0",
  "devDependencies": {
    "bower": "^1.8.0",
    "chromedriver": "^2.27.3",
    "concurrently": "^3.3.0",
    "d2l-galen-utils": "git+https://github.com/Brightspace/d2l-galen-utils.git#v0.2.4",
    "galenframework": "^2.3.2",
    "polylint": "^2.10.0",
    "polymer-cli": "^0.16.0",
    "sauceconnect-runner": "git+https://github.com/Brightspace/sauceconnect-runner.git#v0.3.0",
    "vulcanize": "^1.15.3"
  }
}
